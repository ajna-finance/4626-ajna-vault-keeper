name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  test:
    name: Node ${{ matrix.node }}
    runs-on: ubuntu-latest

    env:
      ORACLE_API_TIER: "demo"
      RPC_URL: "http://127.0.0.1:8545"
      START_ANVIL: false

    strategy:
      fail-fast: false
      matrix:
        node: [22.x]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@8789b3e21e6c11b2697f5eb56eddae542f746c10 # v1, currently v1.7.0

      - name: Install Foundry dependencies
        run: forge install

      - uses: pnpm/action-setup@078e9d416474b29c0c387560859308974f7e9c53 # v6.0.1
        with:
          version: "10.33.0"
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: "pnpm"

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      - name: Type-check
        run: pnpm exec tsc --noEmit

      - name: Lint
        run: pnpm run lint

      - name: Start Anvil and run tests
        env:
          MAINNET_RPC_URL: ${{ secrets.MAINNET_RPC_URL }}
          ORACLE_API_KEY: ${{ secrets.ORACLE_API_KEY }}
          PRIVATE_KEY: "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
        run: |
          nohup anvil --fork-url "$MAINNET_RPC_URL" --fork-block-number 23227726 --port 8545 > anvil.log 2>&1 &
          for i in {1..30}; do
            if curl -s http://127.0.0.1:8545 >/dev/null; then break; fi
            sleep 1
          done
          pnpm run test

      - name: Build
        run: pnpm run build
